/target
/target-test
.claude/
.obsidian/
# demo-seed credentials output (contains session tokens / recovery phrases)
demo-credentials.json
**/*.db
**/*.db-shm
**/*.db-wal
hub_identity.json
*.pem
*.key
.env
# Tantivy search index (runtime artifact)
**/*.search/
# Default upload/soundboard storage dir (WAVVON_UPLOADS_DIR); also where
# `cargo test`'s default cwd writes files for tests that hit upload routes
# without overriding the env var.
crates/hub/uploads/
